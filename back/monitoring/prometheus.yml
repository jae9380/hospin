global:
  scrape_interval: 15s
  evaluation_interval: 15s  # 알람 룰 평가 간격 (옵션)

scrape_configs:
  - job_name: 'HosPin'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']  # Docker에서 로컬 Spring Boot 접근